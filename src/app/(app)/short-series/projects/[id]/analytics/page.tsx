// Minimal server page — no client imports, tests if generateStaticParams is seen.
export function generateStaticParams() {
  return [];
}

export default function AnalyticsPage() {
  return <div>Analytics</div>;
}
